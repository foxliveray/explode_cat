# 🎮 炸弹猫 - 局域网多人游戏部署指南

本指南帮助你在局域网内让多个设备（电脑、手机、平板等）一起玩炸弹猫。

## 📋 前提条件

- 所有设备必须连接到**同一个 WiFi 网络**
- 确保电脑防火墙允许 3001 和 5173 端口的连接

## 🚀 快速开始

### 1. 获取电脑的局域网 IP 地址

在终端运行：

```bash
# macOS
ipconfig getifaddr en0

# 如果上面的命令没有输出,尝试:
ifconfig | grep "inet " | grep -v 127.0.0.1
```

记下显示的 IP 地址,例如 `192.168.1.100`

### 2. 启动服务器

```bash
cd server
npm run dev
```

服务器将在 `http://0.0.0.0:3001` 启动

### 3. 启动前端（需要监听所有网络接口）

```bash
cd client
npm run dev -- --host
```

前端将在 `http://0.0.0.0:5173` 启动

### 4. 在各设备上访问游戏

| 设备          | 访问地址                                    |
| ------------- | ------------------------------------------- |
| 本机电脑      | `http://localhost:5173`                     |
| 手机/其他设备 | `http://192.168.1.100:5173` (替换为你的 IP) |

## 🎲 开始游戏

1. 在一台设备上**创建房间**，记下 6 位房间码
2. 在其他设备上**加入房间**，输入房间码
3. 所有玩家**准备就绪**后，房主点击开始

## ❗ 常见问题

### 端口被占用 (EADDRINUSE)

```bash
# 查找占用3001端口的进程
lsof -i :3001

# 杀死该进程 (替换PID为实际进程ID)
kill -9 <PID>
```

### 手机无法连接

1. 确认手机和电脑在同一 WiFi
2. 尝试关闭电脑防火墙或添加端口例外
3. 确认使用的是局域网 IP 而非 localhost

### 页面加载但无法创建/加入房间

检查浏览器控制台是否有 WebSocket 连接错误，确认 3001 端口服务器正常运行。

## 🔧 技术说明

- 前端会自动使用当前页面的 hostname 连接 WebSocket 服务器
- 服务器 CORS 已配置为允许任意来源连接
- 推荐浏览器：Chrome、Safari、Edge

## 📱 手机最佳体验

- 使用 Chrome 或 Safari 浏览器
- 将网页添加到主屏幕获得类 APP 体验
- 保持屏幕常亮以防断连
